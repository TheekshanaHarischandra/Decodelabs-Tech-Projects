Firstly define a variable as marks and assign 0 value to it and from print function ask the first question and get the user input as a string input and assign it to variable N. After that in if else loop check the user input is correct. Checks for two methods first one for standerd method for English writing and other is all letters in lower case. If the user input correct marks add by 1 and goes to second question. if the user input is wrong print a message saying "Wrong answer" and goes to second question. For second and third questions happens same method and end of the code shows the total marks that user got which means how may correct answers he gives for the questions.