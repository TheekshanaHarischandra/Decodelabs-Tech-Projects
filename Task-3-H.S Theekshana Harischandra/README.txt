first import the random and string libraries 

then getting a user input as length for the password length

After (characters = string.ascii_letters + string.digits) from this code characters assign by the all English letters and digits 0 to 9 

After that defined a password variable and in for loop password assign random value from characters at once it is continue times for the user entered length. After the loop runs equal times as user entered length it stops the execution and show the generated random password